pulumi>=3.0.0
pulumi-aws>=6.0.0
requests>=2.28.0
boto3>=1.26.0
# Local package in editable mode
# NOTE: Installed via deploy.sh with: pip install -e ../../
# This ensures we use the local InfraUtilX package